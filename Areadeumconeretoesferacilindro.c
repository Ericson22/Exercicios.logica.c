#include <stdio.h>
#include <math.h>

main ( ) {

float vol, A, R, H;

char op

printf("cone, cilindro ou esfera?\n");

scanf("%c", &op);

switch(op) {

case char == cone;

printf ("o raio e a altura");

scanf ("%f%f", &r &H);

vol = 3.14*r*r*(H/3);

a = 3.14*r*sgrt((r*r) + (H*H));


printf ("volume = %f e area = %f, vol, A);

break;

case char == cilindro;

printf ("o raio e a atura?");

scanf ("%f%f", &r&H);

vol = 3.14 *r*r*H;

A= 2*3.14*r*H;

printf ("volume = %f e area = %f, vol, A);

break;

case char == esfera;

printf ("o raio e a atura?");

scanf ("%f%f", &r&H);

vol = 3.14 *r*r*r;

A= 2*3.14*r*r;

printf ("volume = %f e area = %f, vol, A);

break;

default;

printf ("opção, invalida. Qual a figura?");

scanf("%c", &op");

break

} 

sistem ("pause");


}
